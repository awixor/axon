# Current Feature

## Status

Not Started

## Goals

## Notes

## History

- Project setup and boilerplate cleanup
- Added mock data (src/lib/mock-data.ts) for dashboard UI
- Dashboard UI Phase 1: ShadCN init, /dashboard route, dark mode, top bar with search and New Item button, sidebar/main placeholders
- Dashboard UI Phase 2: Collapsible sidebar, item types nav, favorite/recent spaces, user avatar area, mobile drawer
- Dashboard UI Phase 3: Stats cards, recent collections, pinned items, recent items grid with type filter tabs
- Prisma 7 + Neon PostgreSQL: schema, initial migration, seed (3 users, 6 spaces, 12 items), Prisma client singleton, db:\* scripts
- Schema hardening: Item gains metadata (JSONB), deletedAt (soft delete), visibility, optional lastEditedById
- Personal space schema: Space gains isPersonal + userId FK; seed-spec updated with 3 default spaces + personal space per user
- Seed demo dataset: rewrote seed.ts with Axon Demo Team, demo@axon.so admin user, 3 team spaces, 1 personal space, 11 items per seed-spec; cleanup block ensures re-runs are clean; test-db.ts updated with full assertions
- Dashboard Spaces: created src/lib/db/spaces.ts (getRecentSpaces), updated dashboard/page.tsx to server component, replaced mock Recent Collections with real SpaceCard grid + All/Team/Personal filter tabs; removed Collections StatCard; renamed to Spaces throughout
- Dashboard Items: created src/lib/db/items.ts (getRecentItems, getPinnedItems, getItemCounts), RecentItems component, time.ts helper, ItemRow type; replaced all mock item data with real Prisma queries; merged feature/dashboard-real-items into main
- Stats & Sidebar: extended getItemCounts (verified + recentActivity), replaced Spaces stat card with Verified Items + Recent Activity; replaced mock spaces and user in sidebar with real DB data (SpaceRow[] from layout, UserRow via getUser); added "View all spaces" link; created src/lib/db/users.ts
- Code Quality Quick Wins: removed 'use client' from DashboardMain, removed dotenv/config from prisma.ts, deleted dead mock-data.ts, fixed O(n×7) typeCounts scan to single-pass reduce in RecentItems, derived Sidebar ITEM_TYPES from TYPE_CONFIG, narrowed NavItem iconStyle to iconColor string
- Auth Setup: installed next-auth@beta + @auth/prisma-adapter, split auth config for edge compatibility (auth.config.ts + auth.ts), added GitHub OAuth provider, protected /dashboard/* via proxy middleware, extended Session type with user.id
- Auth Credentials: added password field to User model, Credentials provider (split pattern: placeholder in auth.config.ts, bcrypt validation in auth.ts), POST /api/auth/register endpoint with validation and error handling, installed bcryptjs
- Auth UI: custom /login page with email/password + GitHub OAuth, /register page with validation and success toast; UserAvatar component (GitHub image or initials fallback); sidebar UserMenu dropdown with profile link and sign-out; dashboard layout now reads real session user instead of hardcoded demo ID
- Email Verification: verificationToken + verificationTokenExpiry on User model; Resend sends 24h link on register; GET /api/auth/verify-email validates token and sets emailVerified; unverified credential users blocked from sign-in; /verify-email "check your email" page; login shows success/error toasts for verification state
- Email Verification Feature Flag: added EMAIL_VERIFICATION_ENABLED env var (default false); register skips token/Resend email and sets emailVerified immediately when disabled; sign-in unverified check gated on same flag; .env.example updated
- Forgot Password: /forgot-password email form + /reset-password?token= form; POST /api/auth/forgot-password stores 1h token in VerificationToken (identifier prefix password-reset:<email>), emails via Resend; POST /api/auth/reset-password validates token, bcrypt-updates password, deletes token; email enumeration prevented; "Forgot password?" link on sign-in; reset=true success toast
- Profile Page: /profile route (auth-guarded layout); displays user info (avatar, name, email, join date); usage stats (total items, total spaces); item counts by type (all 7 types); change password form (credential users only); delete account with inline DELETE confirmation; POST /api/auth/change-password + DELETE /api/auth/delete-account API routes; getProfileData added to src/lib/db/users.ts
- Rate Limiting for Auth: installed @upstash/ratelimit + @upstash/redis; created src/lib/rate-limit.ts (sliding window, fail-open); login (5/15min IP+email via CredentialsSignin), register (3/hr IP), forgot-password (3/hr IP), reset-password (5/15min IP); 429 + Retry-After header; user-friendly errors on all auth forms
- Fix GitHub OAuth Redirect: created src/actions/auth.ts with signInWithGitHub server action (signIn from @/auth, redirectTo v5 pattern); replaced client-side onClick in SignInForm with <form action={signInWithGitHub}>; eliminates double-click sign-in bug
- Items List View: dynamic route /items/[type] with type-filtered Prisma query (getItemsByType), ITEM_TYPE_BY_SLUG slug mapping, items layout mirroring dashboard, proxy extended to /items/:path*; added src/lib/session.ts (React.cache wrapper for auth()) to eliminate duplicate auth() calls across layout+page in items and profile routes
- Vitest setup: installed vitest 4 + @vitest/coverage-v8; vitest.config.ts with node env, native tsconfigPaths, covers src/actions and src/lib; pnpm test / test:run / test:coverage scripts; example tests for cn, relativeTime, getIp, rateLimitResponse (17 tests passing)
- Item listing 3-column layout: added lg:grid-cols-3 to /items/[type] grid — 1 col mobile, 2 col tablet, 3 col desktop+
- Item Drawer: shadcn Sheet slides in from right on ItemCard click; ItemsGrid client wrapper manages state; /api/items/[id] fetches detail with auth+teamId scope; skeleton loading + error state; action bar (Favorite/Pin/Copy/Edit/Delete); works on dashboard and /items/[type] pages
- Item Content Rendering: per-type content display in drawer — SNIPPET with syntax highlighting + language badge (stored in metadata.language), RUNBOOK as markdown with terminal-style code blocks, DOC as full GFM markdown, RESOURCE as parsed URL link card + notes, SECRET_REF as key/value table with blur/reveal toggle, BLUEPRINT as styled plain text; installed react-syntax-highlighter + react-markdown + remark-gfm
- Item Drawer Edit Mode: inline edit mode in drawer (same sheet, no navigation); action bar moved below space tags; Save/Cancel in top row during edit; type-specific fields (title all, content textarea, language for SNIPPET, URL+notes for RESOURCE, read-only note for ASSET); updateItem server action with Zod validation; getItemRawMetadata helper for SNIPPET metadata merge; ItemContentRenderers.tsx + ItemDrawerComponents.tsx refactor; 12 new unit tests (29 total)
- Item Delete: soft-delete via deleteItem server action (sets deletedAt); ShadCN AlertDialog confirmation with destructive icon badge, item title in mono pill, compact 2-column footer; success toast + drawer close on confirm; item removed from ItemsGrid localItems immediately; drawer width increased to sm:max-w-2xl; 4 new unit tests (33 total)
- Item Create: New Item button opens two-step Dialog (type picker grid → type-specific form); createItem server action + insertItem DB function handle all 7 types with Zod validation, per-type content/metadata assembly, optional space assignment via ItemSpace; ASSET shows locked/Pro state; ItemsGrid migrated from useEffect/localItems to useOptimistic for delete; getAllSpaces added to spaces.ts; src/components/ui/dialog.tsx created; 13 new unit tests (46 total)
- Code Editor (CodeMirror): replaced react-syntax-highlighter in SnippetContent with @uiw/react-codemirror (vscodeDark theme, macOS dots, copy button, readonly/edit modes, 13-language LANGUAGE_MAP); replaced SNIPPET textarea in EditForm and NewItemDialog; added defaultType prop to NewItemDialog (skips type picker, hides Back button); added NewTypeItemButton on /items/[type] pages; space selector upgraded to toggle pills using CSS custom properties
- Markdown Editor: new MarkdownEditor component (src/components/ui/markdown-editor.tsx) with Write/Preview tabs + Copy button in edit mode, Copy-only header in readonly mode; replaces textareas for RUNBOOK/DOC/BLUEPRINT content and RESOURCE notes across NewItemDialog and EditForm; RunbookContent/DocContent/BlueprintContent use MarkdownEditor readonly; added GFM table support (table/thead/tbody/tr/th/td) to MarkdownContent
- File Upload with Cloudflare R2: ASSET type unlocked; POST /api/upload (auth, MIME/size validation, R2 PutObject); GET /api/files/[...key] download proxy (auth-gated, avoids CORS); FileUpload component (drag-and-drop, XHR progress, client validation); AssetContent renderer (image preview, file info card, download button); R2 cleanup on soft-delete; softDeleteItem returns { deleted, fileUrl }; Content-Disposition header injection fix; 14 new tests (60 total)
- Asset List View: replaced card grid on /items/assets with Drive-style single-column list; ItemRow gains optional assetMeta (fileKey, fileName, fileSize, mimeType) populated by getItemsByType when type is ASSET; new AssetListView component with mimeType-colored icon boxes (image/blue, code/green, archive/amber, doc/red, generic/slate), image thumbnails, monospace filenames/sizes, left-bar selected state, hover-reveal download button, full keyboard nav (tabIndex, Enter/Space, aria-selected), accessible empty state; /items/[type] page renders AssetListView for ASSET, ItemsGrid for all other types
- Space Create: "New Space" button in TopBar; NewSpaceDialog with live preview, 8-color swatch picker (checkmark on selected), visibility option cards (Lock/Globe icons + descriptions); createSpace server action + insertSpace DB function; SPACE_COLOR_PRESETS + SpaceVisibility const object in src/lib/space-config.ts; label.tsx + textarea.tsx UI primitives; revalidatePath("/dashboard") on success; 9 unit tests (69 total)
- Space Assignment on Items: space selector (toggle pills) added to edit drawer (ItemDrawerComponents.tsx EditForm); updateItemSchema gains spaceIds?; dbUpdateItem runs delete+recreate of ItemSpace rows in $transaction with teamId IDOR guard; spaces prop threaded from server pages → DashboardMain → RecentItems / ItemsGrid / AssetListView → ItemDrawer; dashboard page fetches getAllSpaces in parallel; edit drawer pre-seeds selected spaces from item.spaces; create flow (NewItemDialog) verified unchanged; fixed stale test mocks (auth teamId, getUser removal, getItemForUpdate mock added); 3 new unit tests (72 total)
- Spaces Pages: /spaces list page (SpacesGrid client component, All/Team/Personal filter tabs, 3-col responsive grid); /spaces/[id] detail page (space header with color icon + name + count badge + back link, SpaceItemsGrid with per-type filter tabs — tabs hidden when single type, switches to AssetListView for ASSET filter); SpaceCard wrapped in Next.js Link; getAllSpacesWithCount + getSpaceById + getItemsBySpaceId DB functions added; /spaces auth-guarded in proxy.ts; known bug noted: item count badge includes soft-deleted items (pre-existing in getRecentSpaces too)
